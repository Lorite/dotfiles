# Python dependencies for dotfiles agent tooling (shared venv).
# Built by install.sh into ~/.local/share/dotfiles-agents/venv ; removed by uninstall.sh.
#
# paper-scout/fetch_attach.py: drives system Google Chrome via Playwright
# (channel="chrome") to fetch paywalled PDFs through the institutional proxy.
# No browser download is needed (bundled Chromium is unsupported on some distros).
playwright>=1.40

# task-manager: gcalcli reads Google Calendar (agenda/free-slots/conflicts) from the CLI.
# Needs a one-time OAuth (own Google Cloud OAuth client) — see the task-manager agent.
gcalcli>=4.3

# paper-reader: extract_pdf_annotations.py + sync_zotero_obsidian_notes.py read embedded
# PDF annotations (BOOX highlights) from the Zotero linked files in ~/nextcloud/zotero.
# PyMuPDF does the quad->text recovery; the old pypdf path lost ~40% of highlights to
# empty text and reversed multi-line ones. PyMuPDF is AGPL-3.0, fine for local tooling.
pymupdf>=1.24
