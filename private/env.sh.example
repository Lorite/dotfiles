# Private environment variables — copy to env.sh and fill in real values.
# This file is committed; env.sh is gitignored.

# Orin Jetson Ollama endpoint (used by OpenCode / local LLM tools)
export ORIN_OLLAMA_URL="http://<orin-hostname-or-ip>:11434/v1"
